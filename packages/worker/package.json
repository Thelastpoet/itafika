{
	"name": "@itafika/worker",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"predev": "pnpm --filter @itafika/portal build",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"pretest": "pnpm --filter @itafika/portal build",
		"test": "vitest run",
		"typecheck": "tsc --noEmit",
		"cf-typegen": "wrangler types",
		"data:validate": "node scripts/validate-data.mjs",
		"seed:build": "node scripts/build-seed.mjs",
		"db:migrate:local": "wrangler d1 migrations apply itafika --local",
		"db:migrate:remote": "wrangler d1 migrations apply itafika --remote",
		"db:seed:local": "pnpm seed:build && wrangler d1 execute itafika --local --file=seed.sql",
		"db:seed:remote": "pnpm seed:build && wrangler d1 execute itafika --remote --file=seed.sql"
	},
	"dependencies": {
		"@itafika/adapters": "workspace:*",
		"@itafika/core": "workspace:*"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"@types/node": "^25.9.2",
		"csv-parse": "^5.6.0",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.98.0"
	}
}
